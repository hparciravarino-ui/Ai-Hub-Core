# RAG Module

## Public Interface
- `RAGEngine`: Subsystem executing semantic document context augmentation on incoming raw prompts.

## Implementation
Located in `/engines/rag/RAGEngine.ts`. Cleanly injects document contexts inside standard systemic directives.
