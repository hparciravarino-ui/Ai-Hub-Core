# Repository Indexer Module

## Public Interface
- `RepositoryIndexer`: Subsystem dividing repository file structures into chunk maps for contextual retrieval.

## Implementation
Located in `/engines/indexing/RepositoryIndexer.ts`. Highly modular and handles large directory traversal safely.
